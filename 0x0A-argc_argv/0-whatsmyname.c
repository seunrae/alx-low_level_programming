#include <stdio.h>
/**
 * main - check the code
 * @argc: number of arguments
 * @argv: string array
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	if (argc < 1)
	{
	printf("No arguements");
	}
printf("%s\n", argv[0]);
return (0);
}
