#include "main.h"
#include <stdio.h>

/**
 * mul - check the code.
 * @a: input parameter
 * @b: input parameter
 * Return: res product of a and b.
 */

int mul(int a, int b)
{
int res = a * b;
return (res);
}
