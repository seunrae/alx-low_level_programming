#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen - check the code
 * @s: input parameter
 * Return: Always 0.
 */
int _strlen(char *s)
{
int length = strlen(s);
return (length);
}
